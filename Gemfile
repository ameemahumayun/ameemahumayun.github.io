# Local development only. GitHub Pages ignores this and uses its own setup.
# To preview locally: `bundle install` then `bundle exec jekyll serve`.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
